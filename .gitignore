# ---- Secrets & credentials ----
.env
.env.*
!.env.example
*.pem
*.key
kaggle.json
.snowflake/
secrets/
# profiles.yml IS committed here on purpose: it contains only env_var() lookups
# and safe local defaults — no credentials. Real secrets live in .env (ignored).

# ---- Raw / large data ----
data/raw/
data/*.zip
*.duckdb
*.duckdb.wal
*.db

# ---- dbt ----
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml

# ---- Python ----
.venv/
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# ---- OS / IDE ----
.DS_Store
.vscode/
.idea/

# ---- Docker ----
docker-compose.override.yml
